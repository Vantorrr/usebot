{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -lc 'cd server && npm ci && node src/index.js'",
    "healthcheckPath": "/api/health"
  }
}
