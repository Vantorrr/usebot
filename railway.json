{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -lc 'cd server && npm ci && node src/index.js'",
    "healthcheckPath": "/api/health"
  }
}
